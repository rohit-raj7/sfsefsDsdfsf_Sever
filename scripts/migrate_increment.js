import { pool } from '../db.js'; async function up() { const client = await pool.connect(); try { await client.query('ALTER TABLE listener_global_rate_config ADD COLUMN IF NOT EXISTS increment_interval_mins INTEGER DEFAULT 0;'); await client.query('ALTER TABLE listener_global_rate_config ADD COLUMN IF NOT EXISTS payout_increment NUMERIC(10,2) DEFAULT 0.00;'); await client.query('ALTER TABLE listener_global_rate_config ADD COLUMN IF NOT EXISTS max_payout_rate NUMERIC(10,2) DEFAULT 0.00;'); console.log('added cols'); } catch(e) { console.error(e); } finally { client.release(); process.exit(); } } up();